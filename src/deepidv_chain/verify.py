"""Offline bundle verification — placeholder. Real implementation lands in commit 5."""

from __future__ import annotations

from deepidv_chain.types import VerifyResult


def verify_bundle(bundle_bytes: bytes) -> VerifyResult:  # pragma: no cover - replaced in commit 5
    raise NotImplementedError("verify_bundle lands in commit 5")
